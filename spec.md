# 歌詞表示の仕様

## 入力となる Markdown の仕様

### 曲名や何曲目かのナンバリング

見出し2 `""` で表現する

```example
## 2. Ave verum corpus
```

### ページ番号

行頭に <3> や <50> のように <> 内に囲まれた数字で表現する。次のページ番号が出てくるまではそのページ番号のデータとなる。
冒頭でページ番号がない場合は <0> と同義とする。ページ番号は本文表示部分には表示しない。
行頭に練習番号と重複する場合はどちらが先でも後でもOK。

```example
## 2. Ave verum corpus
<1>Ave, ave verum corpus,
natum de maria, virgine vere
```

### 練習番号

行頭に [A] や [1]、[003]のように、[] 内に囲まれた文字列で表現する。冒頭で練習番号がない場合は [0] と同義とする。
行頭に練習番号と重複する場合はどちらが先でも後でもOK。

```example
## 2. Ave verum corpus
[A]<1>Ave, ave verum corpus,
<2>[B]natum de maria, virgine vere
```

### 横幅について

改行しないと入らない行があった場合には改行して表示する。ただし英語やドイツ語など単語区切りがはっきりしている言語の場合、単語の途中では改行しない。
日本語などで単語区切りがわからない場合は横幅いっぱいまで表示しての改行で問題ない。

### 明るさについて

表示の明るさはスマホ側から設定できるようにする

### 一度に表示したい固まり

空行がない固まりは一度に表示する。ディスプレイ内に一度に表示できない場合は、リングのクリック時に下半分をスクロールさせて表示する。
例えばディスプレイに5行表示できるとして、横幅が足りなくて増やされた改行も含めて 7行(L1,L2,L3,L4,L5,L6,L7) の場合は最初は (L1〜L5) が表示され、クリックすると (L3〜L7) が表示される状態に1秒でスクロールする。3回以上に分けないと表示できない場合は1行オーバーラップさせながら5行ずつ表示していく(4行ずつずれていく)。例えば16行の場合は (L1〜L5) (L5〜L9) (L9〜L13) (L12〜L16) に分かれる。なお、ディスプレイに表示できる行数はフォントの大きさや情報量の多少で可変であることに注意。

### 曲名、ページ番号、練習番号の表示

一番下の行を情報表示行として、左隅を練習番号、中央を曲名、右隅をページ番号が表示されるようにする。それぞれの表示の ON/OFF はスマホ側で設定できるようにする。すべてを通して練習番号やページ番号が0のまま、または曲名が存在しないデータの場合は当該項目は強制的に OFF となるようにする(意味がないので)。曲名が長い場合は視認性が良くなるよう冒頭20文字(日本語では10文字)のみ表示する。Even G2 での表示では右寄せができないので計算で合わせる。
現在表示している部分が複数のページ番号や複数の練習番号に跨がる場合は 2-3 や A-B などハイフンで繋げて表示する。
リングをクリックした際に情報表示行の変更された情報がゆっくりと1回だけ点滅して変更されたことがわかるようにする。また、主情報とはっきりと見分けがつくよう、輝度を薄くする。

### リングの操作

UP/DOWN で練習番号ごとに移動するのかページ番号ごとに移動するのかについてはスマホ側から設定する。

- クリック CLICK_EVENT: 次のデータの表示 (次のパラグラフの表示、パラグラフ全体が表示されていない場合は次の表示ブロックのスクロールしての表示)
- UP SCROLL_TOP_EVENT: 前の練習番号またはページ番号の表示
- DOWN SCROLL_BOTTOM_EVENT: 次の練習番号またはページ番号の表示
- DOUBLE_CLICK_EVENT: 直前の SCROLL_EVENT と同じ方向で 10単位の移動をする
